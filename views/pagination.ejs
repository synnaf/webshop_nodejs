<div>
    <% if (productList.length >= 1) { %>
    <div class="pagination">
        <% if (!isFirst && !isSecond) { %>
            <a href="<%= productListRoute %>?genre=<%= genre %>&page=<%= 1 %>">&lt;&lt;</a>
        <% } %>
        <% if (!isFirst) { %>
            <a href="<%= productListRoute %>?genre=<%= genre %>&page=<%= previousPage %>">&lt;</a>
        <% } %>
        <a href="<%= productListRoute %>?genre=<%= genre %>&page=<%= currentPage %>"><%= currentPage %>/<%= lastPage %></a>
        <% if (!isLast) { %>
            <a href="<%= productListRoute %>?genre=<%= genre %>&page=<%= nextPage %>">&gt;</a>
        <% } %>
        <% if (!isLast && !isSecondLast) { %>
            <a href="<%= productListRoute %>?genre=<%= genre %>&page=<%= lastPage %>">&gt;&gt;</a>
        <% } %>
    </div>
    <% } %>
</div>