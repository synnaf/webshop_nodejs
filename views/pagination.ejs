<div>
    <% if (productList.length >= 1) { %>
    <div class="pagination">
        <% if (!isFirst && !isSecond) { %>
            <a href="<%= productListRoute %>?page=<%= 1 %>">&lt;&lt;</a>
        <% } %>
        <% if (!isFirst) { %>
            <a href="<%= productListRoute %>?page=<%= previousPage %>">&lt;</a>
        <% } %>
        <a href="<%= productListRoute %>?page=<%= currentPage %>"><%= currentPage %>/<%= lastPage %></a>
        <% if (!isLast) { %>
            <a href="<%= productListRoute %>?page=<%= nextPage %>">&gt;</a>
        <% } %>
        <% if (!isLast && !isSecondLast) { %>
            <a href="<%= productListRoute %>?page=<%= lastPage %>">&gt;&gt;</a>
        <% } %>
    </div>
    <% } %>
</div>